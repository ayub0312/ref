# Reference ID

A tiny static page that creates a six-character Crockford Base32 reference ID on every page load. The generator uses the browser's Web Crypto API and provides a one-click copy button.

No identifiers are stored or sent to a server.
